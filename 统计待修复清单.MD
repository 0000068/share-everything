# 统计待修复清单

> 更新时间：2026-05-12
> 本轮复核范围：静态入口、CSS、前端运行时、Notion 同构渲染、API、server、scripts、README 与架构文档。
> 约束：不修改 PC 默认 UI；不修改桌面粒子参数和粒子特效。

## 当前统计

- 总条目：40
- 已完成：38
- 待修复：2
- 本轮新增/完成：移动端首页按参考图二重排、手机粒子静态化、移动端列表小标签修复、移动端文章标签 chip、移动端列表性能轻量化、文章页空态归位、SPA JSON-LD 修复、`.top-actions` 收敛、SSR helper 清理、`dev:bg` 旧口径撤回、本地日志清理、空 VS Code 配置清理、dotfile 换行归一化、删除本机残留 `node_modules/`、静态资源版本一致性 smoke check、正式域名受控硬编码 smoke check、移动端布局契约 smoke check。

## 本轮已修复

- [x] 首页移动端 `Share Everything` 保持同一行，按参考图二改为更低、更宽的静态青蓝紫粉渐变；仅在移动端断点和 `html.is-mobile-device-viewport` fallback 下生效。
- [x] 手机首页粒子只绘制静态星点帧；PC 仍保留桌面动态粒子效果。
- [x] 博客列表移动端卡片内的小标签恢复限宽、省略和稳定行高，避免撑出方形卡片。
- [x] 文章页移动端标签改为可换行小 chip，避免标题下方元信息互相挤压；PC 保持原有文本节奏。
- [x] 博客列表移动端跳过首轮卡片 reveal 动画，并为移动端卡片启用 `content-visibility: auto`，降低滚动和绘制压力。
- [x] 移动端文章“返回列表”入口优化为轻量阅读按钮，并修复本地错误空态被挤到首屏下方的问题。
- [x] 修复 SPA/静态文章页 JSON-LD 在无 nonce 时无法创建的问题，同时保留有 nonce 时的安全属性。
- [x] 收敛移动端 `.top-actions`：blog 保留底部轻导航，post 移动端只保留隐藏 dock 规则。
- [x] 撤回之前的 `dev:bg` / `stop:bg` 旧口径，README、架构文档和 smoke check 统一为当前存在的 `npm.cmd run dev`。
- [x] 删除 6 个根目录本地日志空壳，并移除空的 `.vscode/settings.json`。
- [x] `.gitignore` 增加 `.vscode/`，并在 `.gitattributes` 中补充 `.editorconfig`、`.env.example`、`.gitignore` 的 LF 规则。
- [x] 删除本机残留 `node_modules/`；删除前确认 `package.json` 无 dependencies/devDependencies，代码也未引用本地 `node_modules`。
- [x] `.gitignore` 补充 `.local-server.pid`，继续覆盖 `node_modules/`、`.vscode/`、`*.log`、`.DS_Store`、`Thumbs.db`。
- [x] `scripts/smoke-check.mjs` 增加静态 CSS/JS 资源版本一致性检查，要求三个 HTML 只使用同一个 `?v=`；当前已推进到 `?v=20260513-mobile-ui-fix`。
- [x] `scripts/smoke-check.mjs` 增加正式域名白名单检查，允许 `0000068.xyz` 只出现在当前受控 SEO fallback、文档和检查文件中。
- [x] `scripts/smoke-check/mobile-layout.mjs` 增加移动端布局契约检查，覆盖首页标题渐变/单行、博客卡片分类标签可读高度、标题和收藏按钮同排。

## 仍待修复

- [ ] P2：处理静态 HTML 与 `robots.txt` 中硬编码的 `https://www.0000068.xyz`，方便换域名部署。
- [ ] P2：补充真实浏览器/手机自动截图回归脚本；本轮已补轻量 CSS 布局契约，但还没有可重复运行的截图 diff 脚本。

## 本轮验证

- [x] `npm.cmd run check` 通过。
- [x] `git diff --check` 通过。
- [x] `git status --short --ignored` 已记录基线和最终状态；最终不再显示 `!! node_modules/`。
- [x] 内置浏览器移动端 `390x844` 首页复核：`Share Everything` 单行显示，并接近参考图二的标题、搜索条、图标按钮比例。
- [x] 内置浏览器移动端博客页复核：列表页无粒子，小标签/筛选条显示正常。
- [x] 内置浏览器移动端文章页复核：无 dock 覆盖，错误空态在首屏内显示。
- [x] 内置浏览器桌面首页 `1280x720` 复核：PC 粒子仍显示，两次截图存在差异，动态粒子未被改动。

详细条目继续维护在 `FIX_TODO.md`。
