# 同步

> 更新时间：2026-05-13

## 本轮完成

- 移动端首页按参考图继续优化：`Share Everything` 单行、更低，搜索条和三个图标按钮保持轻量比例，手机端标题改为 PC 同款 `title-gradient` 渐变动画。
- 手机首页粒子已禁用，移动端不再渲染 particle canvas；PC 首页动态粒子和默认 UI 保持不变。
- PC 和移动端博客封面加载占位去掉可见笔记本 emoji，改为安静的渐变/细线 fallback。
- 站点图标改为直接使用准稿 `favicon.png`，避免浏览器优先加载不匹配的 `favicon.svg`；静态引用缓存指纹保持 `favicon.png?v=4`。
- 修复博客卡片小标签、文章页标签 chip、文章页返回按钮、文章页错误空态首屏位置。
- 收敛移动端 `.top-actions`：blog 保留轻导航，post 移动端隐藏 dock。
- 修复 SPA/静态文章页 JSON-LD 无 nonce 时无法重建的问题。
- 清理 `dev:bg` / `stop:bg` 旧口径，当前本地开发口径回到 `npm.cmd run dev`。
- 删除 6 个根目录本地日志空壳，并删除空的 `.vscode/settings.json` / 空 `.vscode/` 目录。
- `.gitignore` 增加 `.vscode/`；`.gitattributes` 补充 dotfile LF 归一化规则。
- 删除本机残留 `node_modules/`；删除前确认 `package.json` 无依赖字段，代码无本地 `node_modules` 引用。
- `.gitignore` 补充 `.local-server.pid`，并确认 `node_modules/`、`.vscode/`、`*.log`、`.DS_Store`、`Thumbs.db` 均已覆盖。
- `scripts/smoke-check.mjs` 新增静态 CSS/JS 资源版本一致性检查，当前统一为 `?v=20260513-content-modules`。
- `scripts/smoke-check.mjs` 新增 `0000068.xyz` 受控硬编码白名单检查，防止正式域名扩散到未登记文件。
- `scripts/smoke-check/mobile-layout.mjs` 新增移动端布局契约检查，守住首页标题渐变、首页纵向位置、博客卡片小标签可读性、标题/小尺寸收藏同排行为。
- 按最新参考图继续优化移动端首页：标题略收宽并整体下移；博客卡片右侧收藏按钮恢复为更克制的 26px 小尺寸。
- v4.0 基线继续收敛生产域名配置：新增 `site.config.json`，`server/notion-server.js` 读取它作为 `SITE_URL` fallback；删除静态 `robots.txt`，改由 `/api/robots` 动态输出；Vercel 和本地开发服务器都映射 `/robots.txt`。
- `scripts/smoke-check.mjs` / `routing-vercel.mjs` 新增静态 HTML fallback canonical/OG 与 `site.config.json` 同步检查、动态 robots handler 检查、Vercel 与本地开发服务器 robots 路由检查。
- 新增 `scripts/visual-regression.mjs` 和 `npm.cmd run visual:check`，可启动本地服务并用本机 Chrome/Edge headless 截图复核移动首页、移动总览、移动文章空态和桌面首页。

## 当前待修

- 暂无已确认待修项。后续优化方向：截图基线持久化、真机远程调试接入、静态资源版本指纹构建期替换。

## 验证

- `npm.cmd run check` 通过。
- `npm.cmd run visual:check` 可执行；当前本机 Chrome/Edge headless 截图受限，脚本生成 skipped 报告；`VISUAL_STRICT=1` 会要求 CDP 契约检查可用，视觉断言失败不会降级为截图 fallback。
- `git diff --check` 通过。
- 2026-05-13 v4.0 复查：smoke check 已覆盖 `site.config.json`、动态 `/api/robots`、Vercel `/robots.txt` rewrite、本地开发服务器 `/robots.txt` 映射、静态 HTML fallback canonical/OG 与配置同步。
- `git status --short --ignored` 已记录，最终不再显示 `!! node_modules/`，且没有新增根目录日志文件。
- 内置浏览器复核 `390x844` 移动首页、移动博客页、移动文章页空态，以及 `1280x720` 桌面首页；桌面首页两次截图存在差异，PC 动态粒子仍在。
